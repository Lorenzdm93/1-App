/**
 * Retired in v0.10.2. The prototype-faithful rebuild renders exercise
 * history and progression as native SVG charts inside Screen.tsx; the old
 * two-pose movement figures are gone.
 *
 * Intentionally inert (web-UI deploys can't delete files). Nothing imports it.
 */
export {}
